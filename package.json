{
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cloudinary": "^1.26.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.14",
    "multer": "^1.4.2",
    "multer-gridfs-storage": "^5.0.0",
    "nodemon": "^2.0.7",
    "path": "^0.12.7"
  },
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "start": "node index.js",
    "build": "cd react-blog && npm run build",
    "install-client": "cd react-blog && npm install",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "license": "ISC",
  "engines": {
    "node": "16.x"
  }
}
